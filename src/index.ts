/**
 * A thing that you should know about `foo` is that it’s called `foo`.
 */
export const foo = 123;

/**
 * Here I’m writing about `bar` but I really want to tell you about {@link foo:var}.
 */
export const bar = 456;
